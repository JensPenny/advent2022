use super::super::tools;

fn day_1_a(input: &String) {
 let result = tools::read_file(input);
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}